apiVersion: v1
kind: Service
metadata:
  name: crewai-agents-service
  namespace: crewai
spec:
  selector:
    app: crewai-agents
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
